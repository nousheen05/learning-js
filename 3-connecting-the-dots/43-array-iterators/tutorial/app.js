// Powerfull Array Methods
// forEach, map, filter, find, reduce
// These methods Iterate over array - no for loop is required
// all these methods Accept CALLBACK function as an argument, calls Callback against each item in a array. 
// Reference Item in the Callback Paramater.

const numbers = [0, 1, 2, 3, 4];

// show all numbers

for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
